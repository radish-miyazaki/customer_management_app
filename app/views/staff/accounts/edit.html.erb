<% @title = "アカウント情報編集"%>
<h2><%= @title %></h2>

<div>
  <%= form_with model: @staff_member, url: staff_account do |f| %>
    <%= render "form", f: f %>
    <div>
      <%= f.submit "更新" %>
      <%= link_to "キャンセル", :staff_account %>
    </div>
  <% end %>
</div>